# REF-001 — Repair eval harness hook script path

## Goal
Make the eval suite execute the real hook script and **fail loudly** if it can’t run, so refactors don’t ship on false-green signals.

## Blockers / Dependencies
- None (this unblocks everything else).

## Files
- `test/evals/run-evals.sh`

## Work (Tasks)
1. Point `HOOK_SCRIPT` at `hooks/claude-judge-continuation.sh` (not `../../scripts/...`).
2. Add preflight checks: exists + executable.
3. Treat “hook execution failed / invalid JSON output” as a **failed run** (not “STOP”).
4. Keep scenario semantics unchanged (only the harness behavior changes).

## Acceptance Criteria
- [ ] `./test/evals/run-evals.sh` fails fast if the hook script path is wrong.
- [ ] Non-zero hook exit or invalid stdout JSON fails the scenario.
- [ ] Existing scenario expectations remain unchanged.

## Proving Commands
- `./test/evals/run-evals.sh`

## Notes
- This is the foundational correctness gate for all subsequent refactors.

