# Phase 2 Plan 1: Structured v2 Output Schema

**Extend judge output with confidence, decision_category, signals, and risk_level fields**

## Tasks

### Task 1: Update JSON Schema and System Prompt
**File:** `hooks/lib/judge.sh`

**Changes:**
1. Expand `JUDGE_JSON_SCHEMA` (line 8) to include v2 fields:
   - `confidence` (number, 0-1)
   - `decision_category` (enum)
   - `signals` (array of enum values)
   - `risk_level` (enum: low/medium/high)
   - `reasons` (array of strings)
   - `next_action` (string, optional)
   - `constraints` (array, optional)
2. Update `JUDGE_SYSTEM_PROMPT` (line 10) to instruct evaluator about new fields
3. Update `build_evaluation_prompt()` to include v2 field instructions

**Verification:** Test with mock data, verify JSON output includes new fields

### Task 2: Update CLI for v2 Field Rendering
**File:** `scripts/explain.sh`

**Changes:**
1. Extend `--verbose` output to render v2 fields when present
2. Detect presence of `confidence`, `decision_category`, `signals`, `risk_level`
3. Keep backward compatible (show "N/A" if fields missing)
4. Format signals as bullet list

**Verification:** Run `./scripts/explain.sh --verbose` with sample v2 decision

### Task 3: Add v2 Test Scenarios
**Files:** `test/evals/scenarios/76-*.json` through `80-*.json`

**Create 5 scenarios:**
1. `76-v2-low-confidence-edge.json` - Ambiguous case with confidence ~0.6-0.75
2. `77-v2-high-risk-continuation.json` - Valid continuation with risk_level=high
3. `78-v2-multi-signal.json` - Multiple signals detected
4. `79-v2-uncertain-category.json` - Edge case decision
5. `80-v2-high-confidence.json` - Clear decision with confidence >0.85

**Verification:** Scenarios validate, pass with v2 stub

### Task 4: Run Full Eval Suite
**Command:** `./test/evals/run-evals.sh`

**Verification:**
- All 80 scenarios pass 5/5 runs
- Snapshot test validates schema consistency
- No regressions on existing 75 scenarios

## Success Criteria

- [ ] Judge returns v2 JSON with confidence, category, signals, risk_level
- [ ] explain.sh --verbose renders v2 fields when present
- [ ] explain.sh still works for v1-only decisions (early exits)
- [ ] All 80 scenarios pass 5/5 runs
- [ ] v1 backward compatibility maintained

## Proving Command

```bash
./test/evals/run-evals.sh
```
