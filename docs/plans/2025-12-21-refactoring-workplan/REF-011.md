# REF-011 — Tighten input validation boundaries

## Goal
Fail fast on malformed hook event JSON and missing required fields, with safe defaults and clear reasons.

## Blockers / Dependencies
- Merge after: REF-001
- Recommended after: REF-003 (single safe emitter).

## Files
- `hooks/claude-judge-continuation.sh`

## Work (Tasks)
1. Validate event JSON early:
   - Required: `session_id` (non-empty)
   - Required: `transcript_path` (non-empty) unless the platform guarantees otherwise
2. Validate `stop_hook_active` defaulting behavior remains unchanged.
3. Ensure validation failures return safe output (approve stop) with a clear reason.

## Acceptance Criteria
- [ ] Required fields are validated before use.
- [ ] Validation failures do not cause stack traces or non-JSON stdout.
- [ ] Eval suite parity remains after harness is fixed.

## Proving Commands
- `./test/evals/run-evals.sh`

