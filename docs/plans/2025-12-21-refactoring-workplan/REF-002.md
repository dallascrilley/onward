# REF-002 — Extract configuration constants into named variables

## Goal
Make policy knobs auditable (throttle window, max continues, transcript lines, model, workdir) without changing behavior.

## Blockers / Dependencies
- Merge after: REF-001 (so behavior parity is provable).

## Files
- `hooks/claude-judge-continuation.sh`

## Work (Tasks)
1. Introduce constants near the top:
   - `MAX_CONTINUATIONS`, `THROTTLE_WINDOW_SECONDS`, `TRANSCRIPT_CONTEXT_LINES`
   - `CLAUDE_MODEL`, `CLAUDE_WORK_DIR`
2. Replace inline literals with these constants (byte-for-byte behavior).
3. Avoid prompt wording changes (only substitute numeric values if already interpolated).

## Acceptance Criteria
- [ ] All policy values are defined once with clear names.
- [ ] No logic changes beyond replacing literals.
- [ ] Eval results match baseline after REF-001 is fixed.
- [ ] `./test/test-working-directory.sh` still passes.

## Proving Commands
- `./test/test-working-directory.sh`
- `./test/evals/run-evals.sh`

