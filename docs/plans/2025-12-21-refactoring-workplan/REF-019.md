# REF-019 — Align docs naming + paths

## Goal
Eliminate documentation drift (`scripts/` vs `hooks/`) so contributors don’t follow broken instructions.

## Blockers / Dependencies
- Coordinate with: REF-001 / REF-007 to ensure the canonical path is correct.

## Files
- `RELENG.md`
- `CLAUDE.md`
- `README.md`

## Work (Tasks)
1. Replace incorrect `scripts/` references with the canonical `hooks/` paths.
2. Ensure the documented test command and hook command match reality.
3. Add a quick grep check to prevent reintroducing `scripts/` references accidentally.

## Acceptance Criteria
- [ ] No docs reference `scripts/` paths that don’t exist.
- [ ] Docs match the actual hook entrypoint (`hooks/claude-judge-continuation.sh`).

## Proving Commands
- `rg -n \"scripts/\" -S .`
- `./test/evals/run-evals.sh`

