# REF-003 — Normalize decision + error output via single emitter

## Goal
Guarantee stdout is always valid JSON with consistent shape, preventing escaping bugs and branch drift.

## Blockers / Dependencies
- Merge after: REF-001

## Files
- `hooks/claude-judge-continuation.sh`

## Work (Tasks)
1. Implement a single output helper (one of):
   - `emit_decision <approve|block> <reason>`
   - or `emit_approve`, `emit_block`, `emit_error`
2. Convert all stdout emission paths to use `jq -n --arg ...` (no raw JSON `echo`).
3. Ensure stderr remains quiet by default (no debug noise).

## Acceptance Criteria
- [ ] No raw JSON `echo '{...}'` remains for stdout.
- [ ] Output contract stays compatible (`decision` + `reason`).
- [ ] Eval suite shows no behavior drift (post REF-001).

## Proving Commands
- `./test/evals/run-evals.sh`
- `./test/test-working-directory.sh`

