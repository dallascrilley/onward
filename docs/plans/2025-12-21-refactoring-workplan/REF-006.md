# REF-006 — Extract throttle logic into focused functions

## Goal
Reduce nesting and duplicated parsing while preserving throttle file semantics exactly.

## Blockers / Dependencies
- Merge after: REF-001
- Strongly recommended after: REF-002 (constants) to keep knobs centralized.

## Files
- `hooks/claude-judge-continuation.sh`

## Work (Tasks)
1. Extract helpers:
   - `throttle_file_for_session`, `throttle_read`, `throttle_write`, `throttle_should_force_stop`, `throttle_clear`
2. Keep the throttle file format unchanged (`<count>:<timestamp>`).
3. Reduce external process usage where safe (e.g., `IFS=: read -r ...`).
4. Flatten control flow with guard clauses (no decision changes).

## Acceptance Criteria
- [ ] Throttle semantics (counts/windows/clear behavior) match baseline.
- [ ] Throttle logic is isolated behind small single-purpose functions.
- [ ] Eval suite shows no behavior drift.

## Proving Commands
- `./test/evals/run-evals.sh`

